{{define "base"}}

<!doctype html>
<html lang='en'>

<head>

  <meta charset='utf-8'>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>{{template "title" .}}
    - MosqueOnline</title>
  <link rel="stylesheet" href="static/css/bootstrap.min.css" media="print" onload="this.media='all'">
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico">
  <script src="static/js/jquery-3.5.1.slim.min.js"></script>
  <script src="static/js/bootstrap.bundle.min.js"></script>
  {{template "scripts" .}}
</head>

<body>
  <header>
    <!-- Irgendein bild oder so -->
  </header>
  <div class="mb-3">
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
        <a class="navbar-brand">
          <img src="/favicon.ico" width="30" height="30" class="d-inline-block align-top" alt=""> MosqueOnline <span class="badge badge-warning">Beta</span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <a class="nav-item nav-link active" href="/admin">Startseite - Adminübersicht<span class="sr-only">(current)</span></a>
            <a class="nav-item nav-link active" href="/logout">Logout<span class="sr-only">(current)</span></a>
          </div>
        </div>
      </nav>
    </div>
  </div>

  <main style="padding-bottom:90px;">
    <div class="container">
      {{template "main" .}}
    </div>
  </main>
  <div class="fixed-bottom">
    <div class="container">
      {{template "footer" .}}
    </div>
  </div>
</body>

</html>

{{end}}
